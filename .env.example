# Regulations.gov API key (optional).
# Without a key the pipeline uses the public DEMO_KEY, which is rate-limited.
# Get a free key at: https://open.gsa.gov/api/regulationsgov/
REGULATIONS_GOV_API_KEY=your_api_key_here
